import MainContainer from './MainContainer';

export function MainHeader() {
  return (
    <header className="fixed top-0 z-50 flex h-40 w-full items-center bg-gradient-to-b from-black/60 to-transparent px-16">
      <MainContainer />
    </header>
  );
}
